#' @param conditionalPowerFunction A user-specified function which calculates the conditional power from the first-stage p-value. This function should not be increasing in the first-stage p-value or monotonicity issues may occur. Note that if a conditional power function is used, the calculated conditional error function is not necessarily the optimal conditional error function. However, we assume that it is still a sufficiently efficient option.
